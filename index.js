const express = require("express") //importato express
const app = express() //inizializzato app express
app.listen(3000, function(){ //messo in ascolto app alla porta 3000 
    console.log("start") 
} ) 

